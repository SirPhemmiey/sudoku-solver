{
  "name": "orderful",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test:unit:watch": "jest --group=unit  --detectOpenHandles --forceExit --verbose  --watchAll --coverage --verbose --config jestconfig.json",
    "test:unit": "jest --group=unit  --detectOpenHandles --forceExit --verbose --coverage --verbose --config jestconfig.json"
  },
  "dependencies": {
    "bunyan": "^1.8.15",
    "express": "^4.18.1",
    "http-status-codes": "^2.2.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "jest": "^27.0.5",
    "ts-node": "^10.4.0",
    "typescript": "~4.3.5"
  },
  "jest": {
    "transform": {
      "^.+[.]tsx?$": "ts-jest"
    }
  }
}
